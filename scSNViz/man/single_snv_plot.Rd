% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/single_snv_plot.R
\name{single_snv_plot}
\alias{single_snv_plot}
\title{Single SNV Plot}
\usage{
single_snv_plot(
  seurat_object,
  processed_snv,
  snv_of_choice = "CHROM:POS:REF:ALT",
  output_dir = NULL,
  slingshot = T,
  dimensionality_reduction = "UMAP",
  dynamic_cell_size = F,
  save_each_plot = F
)
}
\arguments{
\item{seurat_object}{Processed Seurat object.}

\item{processed_snv}{Data frame of processed SNV information, typically output from the preprocess_snv_data function.}

\item{snv_of_choice}{Selected snv}

\item{output_dir}{Directory to save plots and HTML (if save_each_plot is TRUE).}

\item{slingshot}{Logical; whether to include slingshot trajectories. Default: TRUE.}

\item{dimensionality_reduction}{Dimensionality reduction method ('UMAP', 'PCA', 'tSNE'). Default: "UMAP".}

\item{dynamic_cell_size}{Logical; whether to scale cell size dynamically based on SNV and reference read counts. Default: FALSE.}

\item{save_each_plot}{Logical; whether to save each plot individually. Default: FALSE.}
}
\value{
A list containing JSON content for VAF, N_VAR, and N_REF plots.
}
\description{
This function generates individual SNV plots for VAF, N_VAR, and N_REF. It uses processed SNV data
and a Seurat object to create plots with options for saving and including slingshot trajectories.
}
\details{
This function generates an individual SNV plot using processed SNV data (processed_snv) and the dimensionality
reduction embeddings from a Seurat object, and the SNV of interest in the following format, 1:155169447:C:T. The plots visualize key metrics such as VAF (Variant Allele Fraction),
N_VAR (number of variant reads), and N_REF (number of reference reads).

The plot can be saved individually in the specified output_dir if save_each_plot is set to TRUE.
}
\examples{
# Example usage:
one_snv_plot <- single_snv_plot(
  seurat_object = processed_data$SeuratObject,
  processed_snv = processed_data$ProcessedSNV,
  snv_of_choice = "1:155169447:C:T"
  output_dir = "output/individual_plots",
  slingshot = T,
  dimensionality_reduction = "UMAP",
  dynamic_cell_size = F,
  save_each_plot = T
)

}
